/*
package recursion.dp

object MIS extends App{
  def f(a:Array[Int], maxSoFar:Int):Int={
    val currentItem=a.head
    for(i<-1 to a.size-1)
    {
      print()
     /* a(i).map(item=> if(a.head<item) {1 + (a.tail)} else 0)
        .max*/
    }
  }
}
*/
