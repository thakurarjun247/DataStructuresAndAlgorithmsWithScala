package test

import scala.collection.mutable.ListBuffer

object Three extends App {
 /* case class SinglyLinkedListNode(data: Int,  next: SinglyLinkedListNode
     )
  def create = { // TODO: do we really need to initialize this?
    // Yes as we will be setting current.next to some other node, that will throw
    // NPE, it won't even compile with error, variable not initialized
    var current = new SinglyLinkedListNode(0,null)
    // we need to have two separate nodes start and current, current will be
    // incremented every time while start will be used to return in the end.
    val start = current
    val buffer=new ListBuffer[Int]()
  var bufferSize=0
    var i = 0
    while ( {
      i < buffer.size
    }) {
      System.out.println("enter data value")
      //int data = scanner.nextInt();
      val node = new SinglyLinkedListNode(buffer(i),null)
      current.next = node
      current = node


      {
        i += 1; i - 1
      }
    }
    start
  }*/

}
