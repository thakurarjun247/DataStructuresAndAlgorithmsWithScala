/*
package graph
import scala.collection.mutable
class Node{
  var data:Int=0
  var visitStatus:Boolean=false
  var alist=mutable.Buffer[Node]
}

class Graph {
var nodes=List[Node]()
  var isDirected:Boolean=true
/*  def getNode(x:Int):Node={
    nodes.foreach(node=>if(node.data==x) return node)
    else null
  }*/
 /* def addEdge(s:Int,d:Int)=nodes.foreach{
    node=>if(node.data==s) node.alist+=
  }*/

}
*/
